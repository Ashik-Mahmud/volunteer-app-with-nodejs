import React from "react";
import styled from "styled-components";

const Events = () => {
  return <EventsContainer>Events</EventsContainer>;
};
const EventsContainer = styled.section`
  position: relative;
`;
export default Events;
