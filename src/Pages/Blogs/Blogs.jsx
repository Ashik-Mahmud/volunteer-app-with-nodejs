import React from "react";
import styled from "styled-components";

const Blogs = () => {
  return <BlogsContainer>Blogs</BlogsContainer>;
};
const BlogsContainer = styled.section`
  position: relative;
`;
export default Blogs;
